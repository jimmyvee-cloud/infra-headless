# Optional: copy to `.env` here and set to true when you want **zero** delivery API requests — all pages from bundled JSON/TS (home, dev-services, deploy, automate).
# npm run dev picks this up from this directory. Production builds: pass at build time if you need a fully static demo.
# VITE_STATIC_DELIVERY=true
