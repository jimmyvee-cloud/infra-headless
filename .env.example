# Copy to `.env` in this folder (gitignored). Compose reads this for substitution (e.g. WEB_HOST_PORT).
# DynamoDB AWS keys go in `aws-credentials.env` (see aws-credentials.env.example), not here.

WEB_HOST_PORT=5174
